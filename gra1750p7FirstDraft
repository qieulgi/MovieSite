<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>The Substance</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      font-family: 'Arial', sans-serif;
      color: #fff;
      background: url('Untitled design (5).png') repeat;
      background-size: cover;
    }

    header {
      position: relative;
      width: 100%;
      z-index: 2;
    }

    header img {
      width: 100%;
      height: auto;
      display: block;
    }

    nav {
      position: absolute;
      top: 20px;
      right: 20px;
      z-index: 10;
      background-color: #000;
      border-radius: 10px;
      padding: 10px;
      display: flex;
      flex-direction: column;
      align-items: flex-end;
    }

    .nav-toggle {
      background-color: #000; /* Fully black */
      color: #fff;
      padding: 8px 16px;
      border: 2px solid #fff; /* Optional: white outline */
      border-radius: 8px;
      cursor: pointer;
      font-size: 16px;
      font-weight: bold;
      transition: transform 0.2s ease, background-color 0.3s ease;
    }

    .nav-toggle:hover {
      background-color: #222; /* Darker on hover */
      transform: scale(1.05);
    }

    .nav-menu {
      margin-top: 10px;
      display: none;
      flex-direction: column;
      align-items: flex-end;
      gap: 8px;
    }

    .nav-menu a {
      color: #fff;
      background-color: #000;
      padding: 8px 12px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: bold;
      transition: background-color 0.3s, transform 0.3s;
    }

    .nav-menu a:hover {
      background-color: #333;
      transform: scale(1.05) rotate(-1deg);
    }

    .show-menu {
      display: flex;
    }

    /* VIDEO SECTION */
    .video-container {
      position: relative;
      width: 100%;
      padding-top: 56.25%; /* 16:9 ratio */
      margin-top: -5px; /* remove gap under header */
      z-index: 1;
    }

    .video-container iframe {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      border: none;
    }

    section {
      padding: 60px 20px;
    }

    @media (max-width: 768px) {
      nav {
        top: 10px;
        right: 10px;
        padding: 8px;
      }

      .nav-toggle {
        font-size: 14px;
        padding: 6px 12px;
      }

      .nav-menu a {
        font-size: 14px;
      }
    }
  </style>
</head>
<body>

  <!-- HEADER -->
  <header>
    <img src="THE SUBSTANCE.png" alt="The Substance movie title logo">
    <nav>
      <button class="nav-toggle" onclick="toggleMenu()">☰ MENU</button>
      <div class="nav-menu" id="navMenu">
        <a href="#home">Home</a>
        <a href="#trailer">Trailer</a>
        <a href="#gallery">Gallery</a>
        <a href="#merch">Merch</a>
        <a href="#about">About</a>
      </div>
    </nav>
  </header>

  <!-- YOUTUBE VIDEO -->
  <div class="video-container" id="trailer">
    <iframe 
      src="https://www.youtube.com/embed/LNlrGhBpYjc?si=um3_OnK51dOZokQQ" 
      title="The Substance Trailer"
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
      allowfullscreen>
    </iframe>
  </div>

  <!-- CONTENT SECTIONS -->
  <section id="home">
    <h2>Home</h2>
    <p>Welcome to The Substance...</p>
  </section>

  <section id="gallery">
    <h2>Gallery</h2>
    <p>Visuals coming soon...</p>
  </section>

  <section id="merch">
    <h2>Merch</h2>
    <p>Support the experiment. Gear up.</p>
  </section>

  <section id="about">
    <h2>About</h2>
    <p>This is the story of The Substance...</p>
  </section>

  <!-- JS TOGGLE -->
  <script>
    function toggleMenu() {
      const menu = document.getElementById("navMenu");
      menu.classList.toggle("show-menu");
    }
  </script>

</body>
</html>
